apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "trading-engine.fullname" . }}-config
  labels:
    {{- include "trading-engine.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.environment }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
